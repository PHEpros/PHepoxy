{
  "name": "phepoxyworld-aws-backend",
  "version": "1.0.0",
  "description": "AWS Lambda functions and Square sync for PHEpoxyWorld",
  "scripts": {
    "test-square": "node scripts/test-square.js",
    "sync-square": "node scripts/sync-square.js",
    "deploy-lambda": "cd lambda && npm install && npm run package",
    "test": "echo \"Run: npm run test-square\""
  },
  "keywords": [
    "aws",
    "lambda",
    "square",
    "s3",
    "sync"
  ],
  "author": "PHEpoxyWorld",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1500.0",
    "dotenv": "^16.3.1",
    "pdfkit": "^0.13.0",
    "square": "^34.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
